import accountModel from "../models/accounts.model.js"

const accountController = {
    accountRegister: async (req, res) => {
        const { email, password, role } = req.body
        
    }
}

export default accountController